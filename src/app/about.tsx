import { getAboutData } from '../entities/about'; //api 모여있는곳
import Page from '../page/about/page';

async function About(){
  // const auth = await AuthCall();
  // if (!auth){
  //   redirect('/login')
  // }

  // const data = await getAboutData() //api 호출
  // return <Page auth={auth} data={data}/> //props로 데이터 내려보내기
}